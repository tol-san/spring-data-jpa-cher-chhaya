file:
  storage-path: "C:/Users/tolsa/Desktop/upload-image/"
  client-path: "/file"
  base-uri: "http://localhost:${server.port}${file.client-path}/"
spring:
  application:
    name: e-commerce
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
